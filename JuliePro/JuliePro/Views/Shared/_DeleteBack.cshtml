@inject IViewLocalizer Localizer
<div class="row">
    <div class="col">
        <button asp-action="DeletePost" class="btn btn-danger form-control" asp-route-id="@Model">@Locals["Delete"]</button>
    </div>
    <div class="col">
        <a asp-action="Index" class="btn btn-success form-control">@Locals["BackToList"]</a>
    </div>
</div>